package configs;
import java.awt.Color;

public class ColorConfig {

    /**The default background color. */
    public static final Color DEFAULT_BG = new Color(255, 255, 255);

    /**The default contrasting background color. */
    public static final Color DEFAULT_BG_CONTRAST = new Color(13, 6, 40);

    /**The default mediating background color. */
    public static final Color DEFAULT_BG_MID = new Color(0, 176, 240);

    /**The default button background color. */
    public static final Color DEFAULT_BUTTON_BG = new Color(0, 176, 240);

    /**The default contrasting button background color. */
    public static final Color DEFAULT_BUTTON_BG_CONTRAST = new Color(0, 176, 240);
    
    /**The default text color. */
    public static final Color DEFAULT_FG_TEXT = new Color(0, 0, 0);
    
    /**The default contrasting text color. */
    public static final Color DEFAULT_FG_TEXT_CONTRAST = new Color(255, 255, 255);

    /**The default button text color. */
    public static final Color DEFAULT_BUTTON_FG_TEXT = new Color(0, 0, 0);

    /**The default contrasting button text color. */
    public static final Color DEFAULT_BUTTON_FG_TEXT_CONTRAST = new Color(0, 0, 0);
}
